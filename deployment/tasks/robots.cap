namespace :orchestra do
    desc "Generate sitemap.xml files"
    task :generate_robots do
        on roles(:app) do
            execute :php, "#{fetch(:release_path)}/app/console orchestra:robots:generate --env=prod "
        end
    end
    after 'deploy:finishing', 'orchestra:generate_robots'
end
